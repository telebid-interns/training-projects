doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/navbar.css')
    script(type='text/javascript' src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js")

  body
    block content
  include ./navbar.jade
table(style='width:45%')
  tr
    th(align="left") №
    th Name
    th(align="right") Quantity
    th(align="right") Total
  each item in data.cart
      tr(id="row-#{item.id}")
        td #{item.number}
        td #{item.name}
        td(align="right") #{item.quantity}
        td(align="right") $#{item.price * item.quantity}
  tr 
    td Total: 
    td#total $#{data.total}
    td
    td
    td


form.form(method='post')
    select(name='state')
        option(value='0') Awaiting Delivery
        option(value='1') Being Delivered
        option(value='2') Delivered
    input#change(type='submit', name='buybtn', value='Change')